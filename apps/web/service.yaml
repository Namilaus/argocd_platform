apiVersion: v1
kind: Service
metadata:
  name: webservice
spec:
  selector:
    app: web
  ports:
    - port: 8080
      targetPort: http
    - port: 8080
      targetPort: http
    - port: 8080
      targetPort: http
